name: Strict Signature Verification

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  strict-security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set branch name
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            echo "BRANCH_NAME=${{ github.head_ref }}" >> $GITHUB_ENV
          else
            echo "BRANCH_NAME=${{ github.ref_name }}" >> $GITHUB_ENV
          fi

      - name: Strict Security Check
        uses: ICL-ml4csec/signature-trust@v1.1.1
        with:
          repository: ${{ github.repository }}
          branch: ${{ env.BRANCH_NAME }}
          token: ${{ secrets.GITHUB_TOKEN }}
          time-range: "2 weeks"
          key-age-period: "1 month"
          repo-policy: "false:false:false:false:true:false:false"
          deps-policy: "false:false:false:false:true:false:false"
          output-format: "json"
          output-file: "strict-security-report.json"

      - name: Upload verification report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: strict-signature-verification-report
          path: strict-signature-report.json
